package services;

import models.repositories.interfaces.ICommentRepository;

public class CommentService {

    private ICommentRepository repository;


    public void createComment(int authorId, String text)  {
//        Comment comment = new Comment(authorId, );
//        repository.add(authorId, text);
    }
}
